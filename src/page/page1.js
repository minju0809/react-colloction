const Page1 = (props) => {
  return (
    <div className="page1">
      <div>1페이지</div>
      {/* <button
        onClick={() => {
          props.setPage(0);
        }}
      >
        홈으로
      </button>
      <button
        onClick={() => {
          props.setPage(2);
        }}
      >
        2페이지
      </button> */}
    </div>
  );
};

export default Page1;
